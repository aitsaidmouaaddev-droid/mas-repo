export enum MediaVerdict {
  UNKNOWN = 'unknown',
  KEEP = 'keep',
  TRASH = 'trash',
}

export enum AppMediaType {
  PHOTO = 'photo',
  VIDEO = 'video',
}

export enum AppPermissionStatus {
  UNKNOWN = 'unknown',
  GRANTED = 'granted',
  DENIED = 'denied',
}
